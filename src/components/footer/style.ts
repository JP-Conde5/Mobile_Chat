import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    footer:{
        justifyContent: 'space-around',
        alignItems: 'center',
        flexDirection: 'row',
        width: 300, 
    }
})